let valores = [8, 1, 7, 4, 2, 9]

//console.log(valores) mostra o vetor de uma vez//

for (let pos = 0; pos <valores.length; pos++) {
    console.log(`A posição do vetor ${pos} é o valor ${valores[pos]}`)
}